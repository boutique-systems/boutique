name: AI persona review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    uses: boutique-systems/boutique-actions/.github/workflows/ai-persona-review.yml@main
    with:
      level: junior
    secrets: inherit
